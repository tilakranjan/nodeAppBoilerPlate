const demo = require('./demo.js');

// add any new controller here first
// instructions the key should have same name as that of the variable it is being assigned to , this keeps sanity within the code
module.exports = {
  demo:demo
};
